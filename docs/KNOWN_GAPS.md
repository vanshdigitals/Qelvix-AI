# Known gaps

Scope: stubs, mock data, broken endpoints, failing tests, unconfigured providers and unverified paths, in one table.

Effort is a rough size for the change itself: S is under an hour, M is up to a day, L is more than a day.

| # | Item | File | Impact | Effort |
|---|---|---|---|---|
| 1 | RLS does not enforce. The app connects as `postgres`, which has `rolbypassrls = true` and owns every table, and `relforcerowsecurity` is false on all 9 tables. All 22 policies are inert for the API. | `backend/migrations/versions/d3e0543d1ed1_add_rls_policies.py`, `backend/app/database.py:11` | Tenant isolation rests on a single layer. One missing `org_id` filter in a future handler leaks data with no database backstop. | M |
| 2 | The RLS test passes vacuously. It uses `SET ROLE authenticated` and the GUC `request.jwt.claim.org_id`, but `auth.jwt()` reads `request.jwt.claims`, so the predicate compares against NULL and returns zero rows regardless of tenant. It also never exercises the role the app uses. | `backend/tests/test_rls_and_auth.py:96` | The only cross-tenant test proves nothing. | M |
| 3 | `GET /org/me/assets` raises `AttributeError`. Orders by `Asset.created_at`, which is not a column; the table has `first_seen` and `last_seen`. Confirmed by direct attribute access. | `backend/app/routers/org.py:131` | The assets list screen returns 500. | S |
| 4 | `POST /org/me/assets` raises `TypeError`. Passes `source="user"` and `verified=True`, neither of which is a column on `Asset`. Confirmed by direct construction. | `backend/app/routers/org.py:176` | Manual asset creation is impossible. | S |
| 5 | `AssetResponse` requires `source`, `verified` and `created_at`. None exist on the model or the table. | `backend/app/schemas/org.py:35` | Both asset read endpoints fail response validation even after items 3 and 4 are fixed. | S |
| 6 | `GET /org/me/members` orders by `Member.created_at`, which is not a column; the table has `invited_at` and `joined_at`. `MemberResponse` also requires it. | `backend/app/routers/members.py:28`, `backend/app/schemas/org.py:57` | The team screen returns 500. `PUT /members/{id}/role` fails response validation for the same reason. | S |
| 7 | `GET /notifications` orders by `Notification.created_at`, not a column. `NotificationResponse` also requires `recipient` and `created_at`, neither of which is a column. | `backend/app/routers/notifications.py:28`, `backend/app/schemas/notifications.py:15` | 500. Moot in practice, since nothing writes the table. | S |
| 8 | `GET /compliance/latest` orders by `ComplianceReport.created_at`, not a column. `ComplianceReportResponse` requires `framework`, `is_compliant`, `dpdp_narrative` and `created_at`; the table has `clauses`, `overall_status`, `narrative` and `generated_at`. | `backend/app/routers/compliance.py:25`, `backend/app/schemas/compliance.py:7` | Both compliance endpoints are unusable. | S |
| 9 | `GET /org/me` declares `primary_domain: str` while the column is nullable after migration `b7c1a9d2e4f8`. A freshly provisioned org has a null domain. | `backend/app/schemas/org.py:13` | Response validation error, 500, for every user between provisioning and setting a domain. | S |
| 10 | `findings.remediation_steps` is never populated. The worker looks up remediation by `finding_type`; the agent stores it under positional keys `finding_0`, `finding_1`. | `backend/app/worker.py:169`, `backend/app/agents/recovery_recommendation.py:12` | Every LLM-generated remediation is discarded. The report's recommendation branch that would use it is unreachable. | S |
| 11 | Vulnerability analysis finds nothing even with an NVD key. The agent supplies `{"software", "version"}`; the service reads `sw.get("product")` and `continue`s when it is None. | `backend/app/agents/vuln_analysis.py:9`, `backend/app/services/vuln_service.py:21` | CVE detection is dead, and it emits no stub notice either, so nothing surfaces in the report. | S |
| 12 | The CVE severity rule reads `cve.get("cvss")` while the service emits `base_score`. | `backend/app/rules/vuln_rules.py:10`, `backend/app/services/vuln_service.py:60` | Any CVE that got through would be scored 0.0 and classed `low`. | S |
| 13 | VirusTotal findings are unreachable. The rule reads `report.get("positives")`; the service emits `malicious`, `suspicious` and `harmless`. | `backend/app/rules/threat_rules.py:33`, `backend/app/services/threat_service.py:79` | The key is configured and consuming quota, and can never produce a finding. | S |
| 14 | AbuseIPDB is never called. `fetch_threat_intel` is invoked with `[domain]` only, so the IP list is always empty. The rule also reads `confidence` while the service emits `abuse_confidence_score`. | `backend/app/agents/threat_intel.py:11`, `backend/app/services/threat_service.py:109` | The key is configured and unused. | S |
| 15 | The threat-intel "not configured" notice is dead code. `vt_stubbed` is `not threat_data.get("vt_reports")`, and `vt_reports` always holds at least a stub dict. | `backend/app/agents/threat_intel.py:14` | Users are never told VirusTotal and AbuseIPDB are unconfigured. | S |
| 16 | Typosquat detection does not exist. `fetch_phishing_data` always returns `typosquats: []`; no Levenshtein or fuzzy-matching code is present anywhere, despite `rapidfuzz` being a pinned dependency. | `backend/app/services/phishing_service.py:81`, `backend/requirements.txt:54` | Two rules in `phishing_rules.py` are permanently unreachable. A headline capability is absent. | L |
| 17 | Safe Browsing hits are double-counted. The agent emits `safe_browsing_threat` (critical, 25 points) and the rule independently emits `domain_on_safebrowsing` (high, 10 points) for the same match. | `backend/app/agents/phishing_detection.py:29`, `backend/app/rules/phishing_rules.py:36` | One flagged domain contributes 35 points and two findings. | S |
| 18 | Fraud detection is a three-line stub. No ViewDNS client, no `VIEWDNS_API_KEY` setting. | `backend/app/services/fraud_service.py:1` | `fraud_rules.py` is entirely unreachable. Every scan carries one `scanner_not_configured` finding from this agent. | M |
| 19 | WhatsApp send is a stub. The message is generated by the LLM and printed to stdout. `WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_PHONE_NUMBER_ID` and `WHATSAPP_BUSINESS_ACCOUNT_ID` are declared on `Settings` and read by no code. | `backend/app/agents/notification.py:52`, `backend/app/config.py:60` | The stated primary notification channel does not deliver. | M |
| 20 | Email delivery is implemented but unverified. No test imports `email_service` or `notification`. A Resend send failure is caught, printed, and still recorded as sent. | `backend/app/services/email_service.py:40`, `backend/app/agents/notification.py:44` | Delivery failures are invisible and leave no record. Not verified: run a scan for an org with `notification_email` set and check the Resend dashboard for a message id. | M |
| 21 | Nothing writes the `notifications` table. | `backend/app/worker.py:107` | No delivery history exists. `GET /notifications` is always empty. | M |
| 22 | Nothing writes the `compliance_reports` table. DPDP results live only inside `scans.langgraph_state`. | `backend/app/agents/dpdp_compliance.py:19` | Both `/compliance` endpoints always 404. | M |
| 23 | Nothing writes the `audit_log` table. | `backend/app/models/audit.py` | No record of member changes, role changes, org deletion or scan triggers. Contradicts the monitoring claims in `docs/07_SECURITY_COMPLIANCE.md`. | M |
| 24 | The notification payload's severity breakdown is always empty. It reads `state["findings_summary"]`, a key that is not part of `AgentState` and is never written. | `backend/app/agents/notification.py:16` | Emails and WhatsApp messages carry no finding counts. | S |
| 25 | DPDP clause S8.6 always passes. The org context is hardcoded to `{"notification_email": "admin@example.com"}` instead of read from the organization row. | `backend/app/agents/dpdp_compliance.py:11` | A compliance readiness check that cannot fail is reported as passing. | S |
| 26 | The dashboard health band inverts the risk score. `risk_score >= 90` yields band `A`, while `calculate_risk` treats 100 as the worst score. With no completed scan the endpoint substitutes 100. | `backend/app/routers/dashboard.py:32-44`, `backend/app/rules/risk_rules.py:21` | A new or maximally at-risk organization is shown as band `A`. | S |
| 27 | DNS resolver failures become high-severity findings. Every lookup swallows exceptions and returns None or False, so a transient failure is indistinguishable from a genuinely missing record. | `backend/app/services/dns_service.py:88`, `dns_service.py:105`, `dns_service.py:115` | False positives on `no_spf`, `no_dmarc` and `no_dkim`, worth 30 risk points. | M |
| 28 | SPF detection falls back to the first TXT record of any kind when no `v=spf1` string is found. | `backend/app/services/dns_service.py:84` | A domain with any TXT record but no SPF is recorded as having one, suppressing the `no_spf` finding. | S |
| 29 | The SSL Labs poll budget equals the agent node timeout. Both are 90 seconds. | `backend/app/services/ssl_labs_service.py:19`, `backend/app/agents/pipeline.py:29` | A cold assessment hits the node cap first, so the scan gets a degraded-provider entry and no SSL findings, instead of the intended `scanner_unavailable` finding. | S |
| 30 | `risk_scoring` can time out and take the whole score with it. It makes one sequential LLM call per finding before `calculate_risk` runs in the same node, against a 90-second cap. | `backend/app/agents/risk_scoring.py:14-18` | On a scan with many findings the result is a completed scan with no risk score and no explanations. Splitting explanation from scoring would make the score unconditional. | M |
| 31 | LLM outage produces placeholder text rather than an error. Both providers failing returns the literal string "AI service temporarily unavailable (both primary and fallback failed)." | `backend/app/services/claude_service.py:102` | That sentence is stored in `findings.plain_explanation` and shown to users as the explanation. Nothing marks the finding as un-explained. | S |
| 32 | `errors` has no state reducer. Two agents failing in the same parallel superstep both compute the list from the same snapshot, so one entry is lost. | `backend/app/agents/state.py:20`, `backend/app/agents/pipeline.py:45` | `degraded_providers` under-reports concurrent phase-2 failures. Inferred from the absence of `Annotated` reducers; not reproduced. Not verified: force two phase-2 agents to raise and count entries in `scans.langgraph_state->'errors'`. | S |
| 33 | Playbooks are loaded from a path relative to the process working directory. | `backend/app/agents/incident_response.py:10` | Playbooks resolve when the process runs from `backend/` and silently do not when it runs from the repo root. Only 2 playbooks exist; the repo-root `playbooks/` holds only `.gitkeep`. | S |
| 34 | `ir_plan` is computed and never read. No endpoint surfaces it. `tailor_ir_playbook` is defined and called by nothing. | `backend/app/agents/incident_response.py:34`, `backend/app/services/claude_service.py:142` | Incident-response output is inaccessible to users. | M |
| 35 | Two `test_health.py` assertions fail. Both assert the response body is exactly `{status, environment}`, while `HealthResponse` also declares `commit`. Confirmed by inspecting `HealthResponse.model_fields`. | `backend/tests/test_health.py:15`, `test_health.py:23`, `backend/app/main.py:22` | CI gate G2 is red. | S |
| 36 | `test_rls_and_auth.py` requires a live database and reads `backend/.env` at import. CI gate G2 starts an empty Postgres container and never runs migrations. | `backend/tests/test_rls_and_auth.py:17`, `.github/workflows/ci.yml:97` | G2 fails on a missing relation. Run locally, the test seeds and deletes rows in whatever database `DATABASE_URL` points at, which is currently production. | M |
| 37 | `.secrets.baseline` does not exist. Both the pre-commit `detect-secrets` hook and CI gate G7 pass `--baseline .secrets.baseline`. | `.pre-commit-config.yaml:26`, `.github/workflows/ci.yml:200` | G7 is red, so the check that would catch a committed credential never runs. | S |
| 38 | `.env.example` cannot be used as-is. It declares `PHISHTANK_API_KEY` and `SECURITY_TRAILS_API_KEY`, which have no field on `Settings`, and `extra="forbid"` turns an unknown dotenv key into a startup failure. | `.env.example:30-31`, `backend/app/config.py:30` | A new contributor following the documented setup cannot start the backend. | S |
| 39 | `pyyaml` is imported but not declared. It resolves only as a transitive dependency of `langchain-core`. | `backend/app/agents/incident_response.py:3`, `backend/requirements.txt` | A future langchain-core upgrade that drops the dependency breaks the pipeline import, which breaks `POST /scans/trigger`. | S |
| 40 | Celery, Redis, APScheduler and WeasyPrint are pinned and unused. No `.delay()`, `.apply_async()` or `send_task` call exists; APScheduler and WeasyPrint are never imported. | `backend/requirements.txt:15-19`, `backend/requirements.txt:50` | Four dependencies in the supply-chain surface with no runtime purpose, and infrastructure provisioned for them. | S |
| 41 | `DEEPSEEK_API_KEY` is declared on `Settings` and read by no code. A live `sk-` value is present in `backend/.env`. | `backend/app/config.py:37` | A credential is stored for a provider that has no client. | S |
| 42 | `POST /webhooks/scan-status` is unauthenticated. The shared-secret check is commented out. | `backend/app/routers/webhooks.py:28` | A publicly reachable endpoint with no auth. It mutates nothing today, so the exposure is the endpoint itself. | S |
| 43 | `POST /webhooks/whatsapp` does not verify its signature. The header must be present; any value is accepted. The HMAC comparison is commented out. | `backend/app/routers/webhooks.py:15` | Any caller can post to it. It processes nothing today. | M |
| 44 | `org_id` is accepted from `user_metadata`, which is user-writable through `supabase.auth.updateUser`. | `backend/app/dependencies.py:92` | For an account whose `app_metadata.org_id` write failed, a user could set their own `org_id`. Not verified: issue a token with only `user_metadata.org_id` set and call `GET /org/me`. | S |
| 45 | Rate limiting is in-process and in memory. Buckets live in a module-level dict, so limits multiply by instance count and reset on restart. Only two endpoints are limited. | `backend/app/rate_limit.py:20` | Documented as an MVP choice. Any horizontal scaling silently weakens it. | M |
| 46 | The domain verification token never expires and is not single-use. | `backend/app/routers/org.py:208` | A stale token remains valid indefinitely. | S |
| 47 | Pre-existing organizations were grandfathered as domain-verified without a DNS check. | `backend/migrations/versions/b7c1a9d2e4f8_onboarding_state.py:69` | Those tenants can scan a domain they never proved they own. | M |
| 48 | Verification failure responses echo raw DNS data and resolver errors, including the domain's full TXT record set. | `backend/app/routers/org.py:264-273` | Diagnostic detail leaks to the client. Not tenant data, but more than intended. | S |
| 49 | No index on any `org_id`, `scan_id`, `severity`, `status` or `discovered_at` column. Confirmed against `pg_indexes`: every index is constraint-backing. No migration issues a `CREATE INDEX`. | `backend/migrations/versions/` | Every tenant-scoped list query is a sequential scan. `assets` and `members` are covered incidentally by their composite unique indexes. | S |
| 50 | `findings.scan_id` and `findings.asset_id` have no `ON DELETE` clause. | `backend/migrations/versions/f5312e8d14c4_initial_schema.py:117-119` | Deleting a scan or asset with dependent findings raises a foreign-key violation. Org deletion works, because the `org_id` cascade removes findings first. | S |
| 51 | `findings.asset_id` is never written, so findings are not linked to assets. | `backend/app/worker.py:158` | The `asset_id` filter on `GET /findings` always returns empty, and the asset detail screen shows no findings. | M |
| 52 | `assets.first_seen` and `last_seen` are never updated after insert, and `whitelisted` and `metadata` are never read or written. | `backend/app/worker.py:136` | Asset freshness cannot be shown. Whitelisting does not exist. | M |
| 53 | `GET /dashboard/assets` counts `domain`, but the worker only inserts `subdomain` and `ip`. | `backend/app/routers/dashboard.py:97`, `backend/app/worker.py:139` | `total_domains` is always 0. | S |
| 54 | `findings.previous_status` is a column that nothing writes. | `backend/app/routers/findings.py:100` | No status-change history. | S |
| 55 | Provider-unavailable stub findings count toward the risk score at 1 point each. In the default configuration at least two fire on every scan. | `backend/app/rules/risk_rules.py:17`, `backend/app/agents/port_scanner.py:11` | Every organization carries risk points for missing API keys rather than for its own posture. | S |
| 56 | `POST /org/me/members/invite` is a stub that returns success and creates nothing. | `backend/app/routers/members.py:56` | The UI can report an invite that was never sent. | M |
| 57 | `POST /notifications/test` is a stub that returns success and sends nothing. It does not even take a DB session. | `backend/app/routers/notifications.py:52` | The UI can report a test notification that was never sent. | S |
| 58 | `GET /scans/{scan_id}/report.pdf` raises 501. | `backend/app/routers/scans.py:242` | PDF export is unavailable. WeasyPrint is pinned for it and never imported. | L |
| 59 | Scheduled and recurring scans do not exist. APScheduler is installed and never imported; no cron, no schedule table, no `triggered_by="scheduled"` writer. | `backend/requirements.txt:19` | Scanning is manual-trigger only, contradicting `docs/03_BACKEND.md` §6.1. | L |
| 60 | `DELETE /org/me` and `DELETE /org/me/members/{id}` return bare dicts rather than their declared response models. | `backend/app/routers/org.py:117`, `backend/app/routers/members.py:118` | The OpenAPI contract does not match the runtime response. | S |
| 61 | `make seed` invokes `python -m scripts.seed`, and no such module exists under `backend/scripts/`. | `Makefile:38` | The documented seed step fails. | S |
| 62 | Loose scripts named `test_*.py` sit at the backend root and connect to the live database. `backend/` has 5 of them, `scratch/` has 17 more. | `backend/test_pipeline.py`, `backend/test_email.py` | Easy to mistake for the test suite. Running one writes to production. | S |
| 63 | `docs/contracts/openapi.json` and `frontend/lib/api/types.generated.ts` carry pre-implementation prose, for example describing `/scans/trigger` as queuing a Celery task. | `frontend/lib/api/types.generated.ts:470` | Generated client types document behaviour the code does not have. Regenerating will not fix it; the text comes from router docstrings. | S |
| 64 | Frontend API keys, audit log, billing, notifications and team screens have no data source. They render honest "not available yet" panels rather than mock data. | `frontend/components/dashboard/screens/ApiKeysScreen.tsx`, `AuditScreen.tsx`, `BillingScreen.tsx`, `NotificationsScreen.tsx`, `TeamScreen.tsx` | Five navigation entries lead to empty states. Recorded as a gap, not a defect: no fabricated data is shown. | L |
| 65 | `backend/.env` holds live production credentials in the working tree. It is covered by `.gitignore:2` and `git ls-files` reports it untracked, so it has not been committed. | `backend/.env`, `.gitignore:2` | Not a leak. Recorded because the same file is the source of the production `DATABASE_URL` that loose root scripts and `test_rls_and_auth.py` connect to. | S |
| 66 | The LLM boundary is structural, not asserted. No test prevents a future change from feeding model output into a severity or score decision. | `backend/app/agents/risk_scoring.py:18` | The property that `RULES.md` documents would break silently. | S |
| 67 | Whether ruff, ruff-format and mypy currently pass was not established. Neither `.venv` nor `.test_venv` has the tools installed. | `backend/pyproject.toml:7` | G1 status unknown. Not verified: install `requirements-dev.txt` and run the three commands. | S |
| 68 | The backend test suite could not be executed during this review; pytest is absent from both virtual environments. Items 35 and 36 were established by reading code, not by running tests. | `backend/tests/` | Not verified by execution: `pip install -r requirements-dev.txt && pytest`. | S |
| 69 | Five frontend dependencies are declared and never imported. A grep for `@tanstack/react-query`, `zustand`, `react-hook-form`, `zod` and `next-intl` across `frontend/app`, `frontend/components` and `frontend/lib` returns zero files for each. | `frontend/package.json:24`, `:39`, `:34`, `:38`, `:30` | Five unused packages in the bundle and the supply-chain surface. Data fetching is hand-rolled in `lib/api/client.ts`, and no form or validation library is in use. | S |
