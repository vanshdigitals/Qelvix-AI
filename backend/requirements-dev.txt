# Development and CI dependencies. Installed on top of requirements.txt.
-r requirements.txt

# Lint + type-check — gate G1 (08 §12.1)
ruff==0.8.6
mypy==1.14.1

# Tests — gate G2 (08 §12.1), 06 §12
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0

# API test client — 06 §12 (httpx.AsyncClient against the app factory)
asgi-lifespan==2.1.0

# Supply chain — gate G7 (08 §12.1)
pip-audit==2.7.3
detect-secrets==1.5.0

# Typing stubs for mypy strict
types-redis==4.6.0.20241004
celery-types==0.22.0
